---
- name: Install dependencies for Python execution.
  hosts: localhost
  tasks:
  - debug:
      msg: "It's something."
  - name: Install pip dependencies. 
    pip:
      name: "{{ item }}"
      state: latest
    with_items:
    - apache_beam
    - configobj
...
